# Synthetic prototype output — do not use

These Agra CSV, Parquet, JSONL, report, and visualization files were generated by the pre-Novita prototype. Their values include deterministic synthetic mock OCR and are not archival transcriptions.

They are retained only for provenance and regression comparison. Production runs write to `outputs/runs/<run_id>/` and only `SUCCESS` tables may appear under a run's `clean/` directory.
